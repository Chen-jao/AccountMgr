public enum EncryptMode
{
    MD5,
    RSA
}

public enum FileCheckType
{
    Directory,
    File
}

public enum FileCheckMode
{
    Add,
    None
}

public enum EDropDownType
{
    Dir,
    Folder,
    Type,
    Name
}

public enum PannelType
{
    Out,
    Add,
    Result
}

public enum SaveBranch
{
    S_游戏,
    S_社交,
    S_邮箱,
    S_其它
}
